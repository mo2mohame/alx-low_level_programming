#include <stdio.h>
#include <ctype.h>
/**
 * main - function tolower
 *
 * Describtion: replace the char from upper to lower
 *
 * Return: 0 (Success)
 */

int main(void)
{
    int i = 97;
    while (i < 123)
    {
        putchar(i);
        i++;
    }

    putchar(10);
    return (0);
}
