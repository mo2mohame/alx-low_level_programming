#include <stdio.h>
#include <ctype.h>
/**
 * main - function tolower
 *
 * Describtion: replace the char from upper to lower
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char ch = tolower('M');
	putchar(ch);
	return (0);
}
